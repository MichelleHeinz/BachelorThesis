""" Class for majority matrix """
class MajorityMatrix:
    """ Class for majority matrix """
    def __init__(self, matrix):
        """ Initialize matrix """
        self.matrix = matrix
        self.rows = len(matrix)
        self.cols = len(matrix[0])